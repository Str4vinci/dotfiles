# User binding overrides. Sourced from ~/.config/hypr/bindings.conf.
# Lives in dotfiles so omarchy-refresh-config cannot touch it.

# Route screenshots through monthly-folder wrapper (dotfiles/bin/.local/bin/screenshot-monthly)
unbind = , PRINT
bindd = , PRINT, Screenshot (monthly), exec, screenshot-monthly
